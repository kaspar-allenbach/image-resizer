{
  "name": "image-converter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:ghpages": "npm run build && git branch -M gh-pages && git add -A && git commit -m \"deploy\" || true && git push -f origin gh-pages"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "file-saver": "^2.0.5"
  }
}
